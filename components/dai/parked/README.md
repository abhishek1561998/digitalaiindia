# Parked marketing pages

The company marketing homepage and About page. Both were replaced when the
main domain started leading with Learn (`app/page.tsx` and `app/about/page.tsx`
now render the Learn versions).

Kept rather than deleted: putting the marketing site back should be changing
what a route renders, not rewriting the page.
